import 'package:flutter/material.dart';

class Garages extends StatelessWidget {
  const Garages({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Garages Screen')));
  }
}